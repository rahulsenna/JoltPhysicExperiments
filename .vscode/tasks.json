{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build",
            "type": "shell",
            "command": "${workspaceFolder}/1/build.sh",
            "problemMatcher": ["$gcc"],
            "options": {
                "cwd": "${workspaceFolder}/1"
            },
        }
    ]
}